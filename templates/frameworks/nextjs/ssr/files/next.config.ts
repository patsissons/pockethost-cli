import type { NextConfig } from 'next'

const nextConfig: NextConfig = {
  // SSR mode: this frontend deploys to an external host (Vercel, Cloudflare);
  // the PocketHost instance serves only the PocketBase backend.
}

export default nextConfig
